# Tested with Python 3.10 on CUDA 12.1 (RTX 3090, 24 GB).
# The extra index URL pulls the CUDA build of torch / torchvision; change the
# cu121 suffix to match your CUDA toolkit (or drop it for a CPU-only install).
--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.5.1
torchvision==0.20.1
numpy>=1.24
opencv-python>=4.8
pillow>=9.5
